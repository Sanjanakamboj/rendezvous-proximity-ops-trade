[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rendezvous-cw"
version = "0.6.0"
description = "Clohessy-Wiltshire / Hill-frame relative-motion analysis for a servicing-mission rendezvous trade study."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "numpy>=1.24",
    "scipy>=1.11",
]

[project.optional-dependencies]
# pytest: running the test suite. matplotlib: only needed to run the
# scripts/ figure-generation scripts -- the src/rendezvous_cw package
# itself never imports matplotlib.
dev = [
    "pytest>=7.4",
    "matplotlib>=3.7",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
